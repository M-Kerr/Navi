import QtQuick 2.15
Item {

    anchors.fill: parent
    // Implementation is dynamically reloaded for development using the F5 key.
    // Code for the dynamic load is within MapPage.qml.
    DirectionsViewImpl {}
}
